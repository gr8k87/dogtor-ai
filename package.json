
{
  "name": "dogtor-ai",
  "private": true,
  "scripts": {
    "dev": "npm run build:client && concurrently \"npm run build:watch --prefix client\" \"npm run dev --prefix server\"",
    "build:client": "cd client && npm run build",
    "build": "npm run build:client"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "nodemon": "^3.1.10"
  }
}
